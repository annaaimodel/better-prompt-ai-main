# Copy to .env (or set these in your Vercel project settings).
#
# Required: gates the app so only you can draft/parse. Pick any strong string.
ACCESS_CODE=change-me

# Required for AI drafting + lead parsing. Your Anthropic API key.
ANTHROPIC_API_KEY=sk-ant-...
